# Dependencies for the RLVR training skeleton in this directory. These are NOT
# needed to run the evaluation — see ../requirements.txt for that, and keep the
# two in separate environments if you can.
#
# Install torch first, matching your CUDA build:
#   https://pytorch.org/get-started/locally/
torch>=2.4.0
transformers>=4.52.0        # Qwen2_5OmniThinkerForConditionalGeneration
trl>=0.19.0                 # GRPOTrainer with importance_sampling_level="sequence"
accelerate>=1.0.0
deepspeed>=0.15.0           # optional: only if you pass --deepspeed
openai>=1.40.0              # LLM judge client for the text_qa reward
flash-attn>=2.6.0           # optional: --attn_implementation flash_attention_2
